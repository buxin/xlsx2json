---
globs: *.md
description: 文档编写规范和格式
---

# 文档编写规范

## Markdown 格式

### 标题层级
- 使用 `#` 表示主标题
- 使用 `##` 表示章节标题
- 使用 `###` 表示子章节标题

### 代码块
- 使用三个反引号包围代码
- 指定语言类型：```bash, ```javascript, ```json
- 内联代码使用单个反引号

### 列表和链接
- 使用 `-` 或 `*` 创建无序列表
- 使用数字创建有序列表
- 使用 `[文本](链接)` 格式创建链接

### 表情符号
- 使用表情符号增强可读性
- 常用符号：🚀 🎉 ✅ ❌ 📁 📄 🔧 ⚙️

## 文档结构

### README 文件应包含
1. 项目简介
2. 快速开始
3. 安装说明
4. 使用方法
5. 配置说明
6. 故障排除
7. 许可证信息

### 示例格式

```markdown
# 项目名称

## 🚀 快速开始

### 安装
```bash
npm install
```

### 使用
```bash
./bin/xlsx2json auto
```

## 📁 文件说明

- `index.js` - 主程序文件
- `config.json` - 配置文件

## ⚙️ 配置

编辑 `config.json` 文件：

```json
{
  "inputDir": "xlsx",
  "outputDir": "json"
}
```
```